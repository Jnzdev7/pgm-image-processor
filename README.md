# pgm-image-processor
C image processor for PGM (P2) files. Allows loading, saving, and applying basic operations such as thresholding, average pixel calculation, most frequent pixel value, and mean filter. It can also generate striped or random images. Runs through a terminal menu and demonstrates matrix and file manipulation in C.
